/// Enum representing State
enum StateEnum {
  /// Enum representing the initial state
  initial,

  /// Enum representing the loading state
  loading,

  /// Enum representing the completed state
  completed,

  /// Enum representing the error state
  error,
}

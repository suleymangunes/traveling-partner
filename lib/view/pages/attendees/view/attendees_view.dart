import 'package:flutter/material.dart';

class AttendeesView extends StatelessWidget {
  const AttendeesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("this is Attendees page"),
      ),
    );
  }
}
